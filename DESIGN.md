# تسکورا · دیزاین‌سیستم «نئون گلس‌مورفیسم»

کل UI/UX اپ بازطراحی شده و روی یک دیزاین‌سیستم واحد نشسته است.
هیچ پکیج جدیدی اضافه نشده — همه‌چیز با خود Flutter پیاده شده تا `pub get` بدون دردسر بماند.

## ۱) پایه‌ها

| توکن | مقدار | کاربرد |
|---|---|---|
| `NeonPalette.teal` | `#2DD4BF` | رنگ اصلی برند (بنر خوش‌آمدگویی، تب خانه) |
| `NeonPalette.cyan` | `#29B6F6` | تأکید دوم، پروژه‌ها/وظایف |
| `NeonPalette.orange` | `#FF8A3D` | دکمه‌ی CTA اصلی («شروع کار روی وظیفه»)، تب پروفایل |
| `NeonPalette.violet` | `#8B7CF6` | یادآوری‌ها / روز متوالی / تقویم |
| `NeonPalette.lime` | `#34D399` | موفقیت / تکمیل‌شده |
| `NeonPalette.amber` | `#FFB443` | هشدار / یادآوری |
| `NeonPalette.rose` | `#FF5C6C` | خطر / عقب‌افتاده / حذف |
| `abyss / deep / night` | `#060809` … `#12171B` | پس‌زمینه‌ی تیره (نزدیک به سیاه، طبق طرح جدید) |

> این پالت مطابق موکاپ جدید (اسکرین‌شات‌های home-dashboard / tasks-list / task-detail) بازطراحی شد.

شعاع‌ها در `AppRadius` (۱۰ تا ۳۴) و رنگ‌های وابسته به تم در `NeonTokens` (شیشه، متن، هاله).

## ۲) لایه‌ها (این نکته‌ی کلیدی معماری بصری است)

```
NeonBackground (شفق متحرک + شبکه‌ی محو)   ← یک‌بار در app.dart، زیر کل Navigator
      └─ Scaffold با backgroundColor: transparent
             └─ GlassCard ها (BackdropFilter + حاشیه‌ی نوری + گلو)
```

چون پس‌زمینه یک‌بار رسم می‌شود، بلور شیشه در همه‌ی صفحه‌ها واقعی است و هزینه‌ی رندر پایین می‌ماند
(`RepaintBoundary` روی لایه‌ی شفق).

## ۳) کامپوننت‌های جدید (`lib/widgets/neon/`)

- `glass.dart` → `GlassCard`, `GlassAppBar`, `GlassIconButton`, `NeonChip`, `NeonTag`, `SectionHeader`
- `neon_button.dart` → `NeonButton` (گرادیان + گلو + هپتیک + لودینگ)، `NeonOutlineButton`، `NeonFab` (هاله‌ی تپنده)
- `neon_nav_bar.dart` → نوار ناوبری شیشه‌ای شناور، هر تب رنگ نئونی خودش را دارد
- `neon_progress_ring.dart` → حلقه‌ی پیشرفت با گرادیان چرخشی و هاله
- `neon_background.dart` → شفق متحرک (`CustomPainter` + `MaskFilter.blur`)
- `motion.dart` → `FadeSlideIn` (ورود پلکانی لیست‌ها) و `neonRoute` (ترنزیشن اختصاصی صفحه)

## ۴) تغییرات UX (نه فقط ظاهر)

- **داشبورد**: کارت «تمرکز روز» با حلقه‌ی پیشرفت، سلام وابسته به ساعت، شمارنده‌های انیمیشنی، بخش‌های رنگ‌کدشده.
- **لیست وظایف**: جست‌وجوی زنده، فیلتر وضعیت + دسته، **مرتب‌سازی** (موعد/اولویت/تازه‌ترین)، **سوایپ برای حذف** با تأیید.
- **کارت وظیفه**: ریل نئونی اولویت، چک‌باکس انیمیشنی، تاریخ هوشمند («امروز/فردا/دیروز»)، نشانگر تعداد یادآوری.
- **فرم وظیفه**: چیپ‌های میان‌بر تاریخ (امروز/فردا/هفته‌ی بعد)، پاک‌کردن ساعت، بلوک‌بندی واضح.
- **تقویم**: نقطه‌های چندتایی با رنگ معنادار (سبز=همه انجام‌شده، قرمز=عقب‌افتاده)، «پرش به امروز»، هپتیک.
- **ثبت‌نام**: نوار قدرت رمز عبور.
- **تنظیمات**: افزودن دسته‌بندی با **انتخاب رنگ** در بات‌شیت شیشه‌ای.
- **اعداد فارسی** در کل اپ (`utils/fa_num.dart`).
- تم پیش‌فرض روی **نئون شب** است؛ نسخه‌ی روز هم کامل پیاده شده.
- اسپلش‌اسکرین اندروید هم‌رنگ اپ شد (بدون فلش سفید هنگام اجرا).

## ۵) فونت

تم روی خانواده‌ی `Vazirmatn` تنظیم شده. اگر فونت را اضافه نکنی اپ سالم اجرا می‌شود
(فونت پیش‌فرض سیستم)، ولی برای نتیجه‌ی نهایی حتماً اضافه‌اش کن — راهنمای کامل داخل `pubspec.yaml` کامنت شده است.

## ۶) اجرا

```bash
flutter pub get
flutter run
```

اگر روی دستگاه‌های خیلی ضعیف تست کردی و انیمیشن شفق سنگین بود،
در `NeonBackground` مقدار `MaskFilter.blur` را از ۹۰ به ۶۰ کم کن یا `duration` را بالا ببر.
